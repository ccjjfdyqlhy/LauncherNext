
#按照提示内容填入信息。确保所有<>字段都填写正确且提示被完全覆盖。

[app]
class = <是什么格式的应用程序？选择其一：exe, jar, minecraft, py>
exec = <程序的主入口点路径>
runtime = <可选的运行时可执行文件路径，仅当class为exe或jar时需要>
vcwd = <可选的虚拟环境目录，仅当class为py时需要>
author = <作者名>
version = <版本号>
description = <一句话说明你的实例功能>

[config]
exec = <用户可交互的配置程序路径>
requirements = <启动实例所需的依赖项清单文件路径，仅当class为py时需要>

[minecraft] #仅当class为minecraft时需要
ismodpack = <是否为整合包，选择其一：True/False>
version = <Minecraft版本>
isolated = <是否为隔离模式，选择其一：True/False>
